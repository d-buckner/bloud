# Let the rootless Traefik container bind port 80 (its canonical entrypoint)
# in the host network namespace. Without this a rootless container cannot bind
# a port below 1024, and the dev VMs set the same value at provisioning time
# (see dev/lima.yaml).
net.ipv4.ip_unprivileged_port_start=0
