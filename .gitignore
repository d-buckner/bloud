# direnv
.direnv/
.playwrite-mcp/

# Node.js / Turborepo / mise

node_modules/
.turbo/
*.log
npm-debug.log*
mise.toml

# Frontend build outputs
build/
dist/
.svelte-kit/

# Go
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
go.work
go.work.sum
services/host-agent/bin/*
.last-run.json

# Local Go build cache (GOCACHE override used by some dev shells)
.gocache/

# CLI binary
bloud
# `go build` in cli/ without -o lands cli/cli here
cli/cli

# Environment
.env
.env.*
!.env.example

# Images
*.img

# OS
.DS_Store
*.iso
Thumbs.db

# Editor
.idea/
.vscode/
*.swp
*.swo
*~

# Bloud local state
.bloud/

# Generated Traefik dynamic routes. Written at runtime into
# BLOUD_TRAEFIK_DYNAMIC_DIR; must never be committed in-tree (a stray
# host-agent boot with the default cwd used to drop it here).
services/host-agent/internal/api/traefik/

# Dolt
.dolt/
*.db
.beads/

# AI agent data
.claude/
.playwright-mcp/

# Vale: `vale sync` installs the styles pinned in .vale.ini into
# .vale/styles/. Our own style and vocabulary live there too, so the directory
# is ignored selectively (same pattern as the docs recommend).
.vale/styles/*
!.vale/styles/Bloud/
!.vale/styles/config/
.vale/styles/config/*
!.vale/styles/config/vocabularies/
.vale/styles/config/vocabularies/*
!.vale/styles/config/vocabularies/Bloud/
