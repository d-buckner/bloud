# Vocabulary for the Bloud docs (`Vocab = Bloud` in .vale.ini).

# One entry per line, each a case-sensitive regular expression (a period is
# literal text, a space also matches a line break).

# This list is small on purpose. `Vale.Spelling` -- the dictionary spell-checker
# that would otherwise flag every technical word and product name -- is disabled
# in `.vale.ini`, so the only entries left here are the ones another rule still
# needs. Do not re-add a word just because it is jargon: that is what turned off
# the spell-checker was for.

# --- Enforced spellings -----------------------------------------------
# Exact case, not `(?i)`: these are proper nouns whose casing is part of the
# name, so any other case raises `Vale.Terms` with this spelling as the fix.
Forgejo
OAuth
PostgreSQL
Tailscale

# --- Jargon kept on purpose -------------------------------------------
# Google.Jargon would rewrite this away. It is the domain term for the recovery
# account and readers search for it, so it stays (see the tech-debt ledger for
# the auth-bypass recovery flow).
break-glass
